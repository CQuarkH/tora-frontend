import 'package:flutter/material.dart';

class ChildCalendarScreen extends StatelessWidget {
  const ChildCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
